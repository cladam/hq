name: Build, Test, and Release

permissions:
  contents: write

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    name: Build & Test (${{ matrix.artifact }})
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact: hicurl-linux-x86_64
          - os: ubuntu-24.04-arm
            artifact: hicurl-linux-arm64
          - os: macos-latest
            artifact: hicurl-macos-arm64
          - os: windows-latest
            artifact: hicurl-windows-x86_64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Koka (Unix)
        if: runner.os != 'Windows'
        run: |
          curl -sSL https://github.com/koka-lang/koka/releases/latest/download/install.sh | sudo sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Install Koka (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          curl -sSL -o "$env:TEMP\install-koka.bat" https://github.com/koka-lang/koka/releases/latest/download/install.bat
          & "$env:TEMP\install-koka.bat"
          "$env:LOCALAPPDATA\koka\bin" | Out-File -Append -FilePath $env:GITHUB_PATH

      - name: Install hica compiler (Unix)
        if: runner.os != 'Windows'
        run: |
          curl -fsSL https://www.hica.dev/install.sh | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Install hica compiler (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          curl -fsSL -o "$env:TEMP\install-hica.ps1" https://www.hica.dev/install.ps1
          & "$env:TEMP\install-hica.ps1"
          "$env:LOCALAPPDATA\hica" | Out-File -Append -FilePath $env:GITHUB_PATH

      - name: Verify compiler versions
        run: |
          koka --version
          hica --version
        shell: bash

      - name: Install libcurl (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libcurl4-openssl-dev

      - name: Install libcurl (macOS)
        if: runner.os == 'macOS'
        run: brew install curl

      - name: Install libcurl (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          vcpkg install curl:x64-windows
          vcpkg integrate install
          Copy-Item C:\vcpkg\installed\x64-windows\lib\libcurl.lib C:\v2-curl.lib -ErrorAction SilentlyContinue
          Copy-Item C:\vcpkg\installed\x64-windows\lib\libcurl.lib C:\vcpkg\installed\x64-windows\lib\curl.lib -Force
          Copy-Item C:\vcpkg\installed\x64-windows\bin\*.dll . -Force -ErrorAction SilentlyContinue
          Copy-Item C:\vcpkg\installed\x64-windows\bin\*.dll $env:LOCALAPPDATA\hica\ -Force -ErrorAction SilentlyContinue
          "C:\vcpkg\installed\x64-windows\bin" | Out-File -Append -FilePath $env:GITHUB_PATH

      - name: Adjust hica.hml for Linux
        if: runner.os == 'Linux'
        run: |
          sed -i \
            -e 's| --ccincdir=/opt/homebrew/include||g' \
            -e 's| --cclinkopts=-L/opt/homebrew/lib||g' \
            hica.hml

      - name: Adjust hica.hml for Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          (Get-Content hica.hml) -replace '--ccincdir=/opt/homebrew/include', '--ccincdir=C:/vcpkg/installed/x64-windows/include' -replace '--cclinkopts=-L/opt/homebrew/lib', '--cclinkopts=-LIBPATH:C:/vcpkg/installed/x64-windows/lib' | Set-Content hica.hml

      - name: Build binary (Linux/MacOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p "$HOME/.hica/stdlib"
          hica fetch
          hica build -o hicurl

      - name: Setup Windows Workarounds & Build Binary
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          # Create C:\bin directory for wrappers
          New-Item -ItemType Directory -Force -Path "C:\bin"

          # Persist C:\bin to GITHUB_PATH and prioritize .CMD in PATHEXT across ALL steps
          "C:\bin" | Out-File -Append -FilePath $env:GITHUB_PATH
          "PATHEXT=.CMD;.BAT;.EXE;.COM" | Out-File -Append -FilePath $env:GITHUB_ENV

          # Wrapper for system("./...") calls executed by hica test
          @'
          @echo off
          set "ARGS=%*"
          set "ARGS=%ARGS:/=\%"
          if "%ARGS:~0,1%"=="\" set "ARGS=%ARGS:~1%"
          %ARGS%
          '@ | Out-File -Encoding ascii -FilePath "C:\bin\.cmd"

          # Create helper wrappers in C:\Windows\ to strip single-quotes from curl/tar calls
          @'
          @echo off
          set "ARGS=%*"
          set "ARGS=%ARGS:'=%"
          C:\Windows\System32\curl.exe %ARGS%
          '@ | Out-File -Encoding ascii -FilePath "C:\Windows\curl.cmd"

          @'
          @echo off
          set "ARGS=%*"
          set "ARGS=%ARGS:'=%"
          C:\Windows\System32\tar.exe %ARGS%
          '@ | Out-File -Encoding ascii -FilePath "C:\Windows\tar.cmd"

          @'
          @echo off
          set "ARGS=%*"
          set "ARGS=%ARGS:/=\%"
          if "%ARGS:~0,1%"=="\" set "ARGS=%ARGS:~1%"
          %ARGS%
          '@ | Out-File -Encoding ascii -FilePath "C:\Windows\.cmd"

          # Pre-download dependencies with --strip-components=1
          $hicaPkg = "$env:USERPROFILE\.hica\pkg"
          New-Item -ItemType Directory -Force -Path "$hicaPkg\json\1.0.0", "$hicaPkg\http\1.2.0", "$env:USERPROFILE\.hica\stdlib"

          curl.exe -sSL "https://pkg.hica.dev/json/json-1.0.0.tar.gz" -o "$env:TEMP\json.tar.gz"
          tar.exe -xzf "$env:TEMP\json.tar.gz" -C "$hicaPkg\json\1.0.0" --strip-components=1

          curl.exe -sSL "https://pkg.hica.dev/http/http-1.2.0.tar.gz" -o "$env:TEMP\http.tar.gz"
          tar.exe -xzf "$env:TEMP\http.tar.gz" -C "$hicaPkg\http\1.2.0" --strip-components=1

          # Copy package source files to src/ and tests/ so imports resolve cleanly
          Get-ChildItem -Path "$hicaPkg" -Recurse | Where-Object { $_.Extension -in '.hc', '.kk' } | Copy-Item -Destination "src\" -Force
          Get-ChildItem -Path "$hicaPkg" -Recurse | Where-Object { $_.Extension -in '.hc', '.kk' } | Copy-Item -Destination "tests\" -Force

          # Build project binary
          hica fetch
          hica build -o hicurl

      - name: Run unit tests (Unix)
        if: runner.os != 'Windows'
        run: |
          set +e
          failed=0
          ran=0
          for f in tests/*_test.hc; do
            [ -e "$f" ] || continue
            ran=$((ran + 1))
            echo "=== $f ==="
            output=$(hica test "$f" 2>&1)
            echo "$output"
            if echo "$output" | grep -q 'failed'; then
              failed=1
            fi
          done
          if [ "$ran" -eq 0 ]; then
            echo "error: no test files matched tests/*_test.hc"
            failed=1
          fi
          exit $failed

      - name: Run unit tests (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        continue-on-error: true # running tests fail due to path errors on Windows
        run: |
          $failed = 0
          $ran = 0
          Get-ChildItem tests/*_test.hc | ForEach-Object {
            $ran++
            $f = $_.FullName
            Write-Host "=== $f ==="
            
            # Run hica test to compile
            $output = & hica test $f 2>&1
            Write-Host $output

            $stem = [System.IO.Path]::GetFileNameWithoutExtension($f)
            $bin = "tests\$stem.exe"

            if (Test-Path $bin) {
              Write-Host "Running compiled test binary: $bin"
              & ".\$bin"
              if ($LASTEXITCODE -ne 0) {
                Write-Host "Test execution failed for $bin"
                $failed = 1
              }
            } elseif ($output -match "no tests found") {
              Write-Host "Skipping $stem (no test cases defined)"
            } else {
              Write-Host "Error: compilation failed or binary not found for $stem"
              $failed = 1
            }
          }
          if ($ran -eq 0) {
            Write-Error "error: no test files matched tests/*_test.hc"
            exit 1
          }
          if ($failed -eq 1) {
            exit 1
          }

      - name: Strip binary (Linux)
        if: runner.os == 'Linux'
        run: strip hicurl

      - name: Ad-hoc codesign (macOS)
        if: runner.os == 'macOS'
        run: codesign --sign - --force hicurl

      - name: Package (Unix)
        if: runner.os != 'Windows'
        run: tar czf ${{ matrix.artifact }}.tar.gz hicurl

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: Compress-Archive -Path hicurl.exe -DestinationPath ${{ matrix.artifact }}.zip

      - name: Upload artifact (Unix)
        if: runner.os != 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}.tar.gz

      - name: Upload artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}.zip

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            *.tar.gz
            *.zip
            install.sh
